/**********************************************************
** Description: Header file for the function that inputs
   user values into the array. There is no return type and
   the function takes the 2d array and the array size as the
   arguments. 
**********************************************************/

#ifndef READMATRIX_HPP
#define READMATRIX_HPP

void readMatrix(int** matrix, int arraySize);

#endif
